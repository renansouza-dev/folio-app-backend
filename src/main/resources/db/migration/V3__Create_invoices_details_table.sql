CREATE TABLE IF NOT EXISTS invoices_details (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   asset VARCHAR(6) NOT NULL,
   operation VARCHAR(255) NOT NULL,
   quantity INTEGER NOT NULL,
   price DECIMAL NOT NULL,
   fees DECIMAL NOT NULL,
   invoice_id BIGINT NOT NULL,
   CONSTRAINT pk_invoices_details PRIMARY KEY (id)
);

ALTER TABLE invoices_details ADD CONSTRAINT FK_INVOICES_DETAILS_ON_INVOICE FOREIGN KEY (invoice_id) REFERENCES invoices (id);